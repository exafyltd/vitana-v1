import { Card } from '@/components/ui/card';

// Visual context removed - will be reinstalled

export const VisualContextControls = () => {
  return (
    <Card className="p-6">
      <p className="text-muted-foreground">Visual context features will be available after reinstallation.</p>
    </Card>
  );
};

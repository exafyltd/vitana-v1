/**
 * VitanaOrbButton — DISABLED
 * Replaced by external ORB voice widget (orb-widget.js)
 * Kept as stub so existing imports don't break.
 */
interface VitanaOrbButtonProps {
  onClick?: () => void;
}

export function VitanaOrbButton({ onClick }: VitanaOrbButtonProps) {
  return null;
}

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript incremental build cache
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Codemod artifacts
scripts/.codemod-*.manifest.json

supabase/.temp/
.git-worktrees/

# RepoWise codebase-intelligence index (local, machine-specific — see .mcp.json note)
.repowise/
.mcp.json
.vscode/mcp.json
.vscode/extensions.json

# Screenshot-harness scratch: the verification script is copied in here so it can resolve
# playwright from node_modules, then deleted. It carries a test credential — never commit it.
.*-shots.tmp.mjs
